# Selenide configuration properties

# Default browser (chrome, firefox, edge, etc.)
browser=chrome

# Default language (vi, en)
language=en

# Run mode: local or grid
mode=local

# Browser window size (width x height)
browserSize=1920x1080

# Base URL
baseUrl.vj=https://www.vietjetair.com/
baseUrl.lf=https://store.leapfrog.com/en-us/apps/c?p=1&platforms=197&product_list_dir=asc&product_list_order=name

# Timeout for finding elements (in milliseconds)
timeout=10000

# URL of Selenium Grid (only used if mode=grid)
gridUrl=false

# Headless mode (true/false)
headless=false

# Page load timeout (in milliseconds)
pageLoadTimeout=30000

# Project folder
reportsFolder=test-result/reports

# Load DOM
pageLoadStrategy=eager
